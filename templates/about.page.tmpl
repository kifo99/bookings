{{template "base".}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is about page</h1>      
                
                <p>This came from template: {{index .StringMap "test"}}</p>

                <p>
                    {{if ne (index .StringMap "remote_ip") ""}}
                        Your remote ip addres is{{index .StringMap "remote_ip"}}
                    {{else}}
                        I dont know your ip addres. Visit the <a href="/">Home page</a> so i can set your ip.
                    {{end}}
                </p>
            </div>
        </div>
    </div>
{{end}}